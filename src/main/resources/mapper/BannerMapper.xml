<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.BannerMapper">

    <resultMap id="BannerResultMap" type="com.example.demo.domain.Banner">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="image_url" property="imageUrl"/>
        <result column="link_type" property="linkType"/>
        <result column="link_value" property="linkValue"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="start_time" property="startTime"/>
        <result column="end_time" property="endTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectById" resultMap="BannerResultMap">
        select * from banner where id = #{id}
    </select>

    <select id="selectAll" resultMap="BannerResultMap">
        select * from banner order by sort_order asc, id asc
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into banner(title, image_url, link_type, link_value, sort_order, status, start_time, end_time)
        values (#{title}, #{imageUrl}, #{linkType}, #{linkValue}, #{sortOrder}, #{status}, #{startTime}, #{endTime})
    </insert>

    <update id="update">
        update banner
        set title      = #{title},
            image_url  = #{imageUrl},
            link_type  = #{linkType},
            link_value = #{linkValue},
            sort_order = #{sortOrder},
            status     = #{status},
            start_time = #{startTime},
            end_time   = #{endTime}
        where id = #{id}
    </update>

    <delete id="delete">
        delete from banner where id = #{id}
    </delete>
</mapper>
