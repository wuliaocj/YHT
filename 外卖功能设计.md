# 外卖功能设计方案

## 一、核心流程设计

### 1. 外卖订单生命周期
```
用户下单 → 订单确认 → 支付 → 商家接单 → 制作 → 配送 → 送达 → 完成
```

### 2. 关键节点
- **下单**：用户选择商品、规格、地址，提交订单
- **支付**：支持微信、支付宝等在线支付
- **商家接单**：商家确认订单并开始制作
- **配送**：分配配送员或通知用户自提
- **送达**：用户确认收货，订单完成

## 二、核心模块设计

### 1. 地址管理模块
- **地址CRUD**：用户添加、修改、删除收货地址
- **默认地址**：支持设置默认收货地址
- **地址验证**：验证地址格式和有效性
- **地址列表**：按用户ID查询地址列表

### 2. 订单管理模块
- **订单创建**：生成订单号、计算金额、关联地址
- **订单状态**：管理订单状态流转（待支付、待接单、制作中、配送中、已完成、已取消）
- **订单查询**：用户查询订单列表和详情
- **订单操作**：取消订单、重新下单

### 3. 配送管理模块
- **配送费计算**：根据距离、重量等计算配送费
- **配送方式**：支持商家配送、第三方配送、自提
- **配送状态**：实时更新配送进度
- **配送员管理**：配送员信息管理和分配

### 4. 支付管理模块
- **支付接口**：集成微信支付、支付宝等支付接口
- **支付状态**：管理支付状态（待支付、已支付、支付失败）
- **支付记录**：记录支付交易信息
- **退款处理**：处理订单退款流程

### 5. 通知模块
- **订单通知**：订单状态变更通知
- **配送通知**：配送进度通知
- **支付通知**：支付结果通知
- **系统通知**：系统消息和活动通知

## 三、数据库设计

### 1. 核心表结构

#### `order` 表（订单表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | INT | 订单ID |
| order_no | VARCHAR(32) | 订单号 |
| user_id | INT | 用户ID |
| total_amount | DECIMAL(10,2) | 总金额 |
| discount_amount | DECIMAL(10,2) | 优惠金额 |
| delivery_fee | DECIMAL(10,2) | 配送费 |
| actual_amount | DECIMAL(10,2) | 实际支付金额 |
| payment_method | INT | 支付方式 |
| payment_status | INT | 支付状态 |
| order_status | INT | 订单状态 |
| order_type | INT | 订单类型（1=堂食，2=外卖） |
| address_id | INT | 地址ID |
| delivery_type | INT | 配送方式（1=商家配送，2=第三方配送，3=自提） |
| deliveryman_id | INT | 配送员ID（可选） |
| take_code | VARCHAR(10) | 取餐码 |
| estimated_time | DATETIME | 预计送达时间 |
| complete_time | DATETIME | 完成时间 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### `address` 表（地址表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | INT | 地址ID |
| user_id | INT | 用户ID |
| consignee | VARCHAR(50) | 收货人姓名 |
| phone | VARCHAR(20) | 联系电话 |
| province | VARCHAR(50) | 省份 |
| city | VARCHAR(50) | 城市 |
| district | VARCHAR(50) | 区县 |
| detail_address | VARCHAR(255) | 详细地址 |
| is_default | TINYINT | 是否默认地址 |
| status | TINYINT | 状态（1=启用，0=禁用） |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### `deliveryman` 表（配送员表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | INT | 配送员ID |
| name | VARCHAR(50) | 配送员姓名 |
| phone | VARCHAR(20) | 联系电话 |
| status | TINYINT | 状态（1=在线，0=离线） |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

#### `order_delivery` 表（配送记录表）
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | INT | 记录ID |
| order_id | INT | 订单ID |
| deliveryman_id | INT | 配送员ID |
| status | INT | 配送状态 |
| current_location | VARCHAR(255) | 当前位置 |
| estimated_arrival_time | DATETIME | 预计到达时间 |
| actual_arrival_time | DATETIME | 实际到达时间 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

## 四、API 设计

### 1. 地址管理 API
- **GET /api/address/list/{userId}**：获取用户地址列表
- **POST /api/address/save**：保存地址（新增或更新）
- **POST /api/address/delete/{id}**：删除地址
- **POST /api/address/setDefault/{id}**：设置默认地址

### 2. 订单管理 API
- **POST /api/order/create**：创建订单
- **GET /api/order/list/{userId}**：获取用户订单列表
- **GET /api/order/{id}**：获取订单详情
- **POST /api/order/cancel/{id}**：取消订单
- **POST /api/order/reorder**：重新下单
- **POST /api/order/confirm/{id}**：确认收货

### 3. 配送管理 API
- **GET /api/delivery/list**：获取配送员列表（管理员）
- **POST /api/delivery/assign**：分配配送员（管理员）
- **POST /api/delivery/updateStatus**：更新配送状态
- **GET /api/delivery/track/{orderId}**：追踪配送进度

### 4. 支付管理 API
- **POST /api/payment/create**：创建支付单
- **POST /api/payment/callback**：支付回调
- **GET /api/payment/status/{orderNo}**：查询支付状态

## 五、技术实现要点

### 1. 订单状态流转
- **使用枚举管理状态**：定义 `OrderStatus` 枚举，包含所有订单状态
- **状态转换验证**：确保状态流转符合业务逻辑（如待支付 → 已支付 → 待接单）
- **状态变更通知**：状态变更时通过WebSocket通知前端

### 2. 配送费计算
- **基础配送费**：设置默认配送费
- **距离计算**：根据用户地址与商家距离计算配送费
- **重量计算**：根据订单商品总重量计算额外配送费
- **优惠活动**：支持免配送费活动

### 3. 地址验证
- **格式验证**：验证手机号、详细地址格式
- **坐标转换**：将地址转换为经纬度，用于距离计算
- **地址库匹配**：与地图API集成，验证地址有效性

### 4. 实时通知
- **WebSocket**：用于实时推送订单状态和配送进度
- **消息队列**：处理异步通知，如订单状态变更、配送提醒
- **短信通知**：重要节点通过短信通知用户

### 5. 异常处理
- **订单超时**：处理支付超时、接单超时等情况
- **配送异常**：处理配送延迟、配送失败等情况
- **系统故障**：确保系统故障时订单数据的一致性

## 六、前端交互设计

### 1. 下单流程
1. **商品选择**：用户浏览商品，选择规格
2. **地址选择**：选择或新增收货地址
3. **订单确认**：显示商品清单、金额、配送费
4. **支付**：跳转到支付页面完成支付
5. **订单状态**：显示订单状态和预计送达时间

### 2. 订单追踪
- **实时地图**：显示配送员位置和配送路线
- **状态更新**：实时更新订单状态
- **预计时间**：显示预计送达时间和倒计时

### 3. 地址管理
- **地址列表**：显示用户保存的地址
- **地址编辑**：支持修改和删除地址
- **地址搜索**：通过地图API搜索地址

## 七、扩展性设计

### 1. 多商家支持
- **商家管理**：支持多个商家入驻
- **订单分配**：根据商品归属分配订单给对应商家
- **配送范围**：每个商家设置配送范围

### 2. 多配送方式
- **商家自配**：商家自己配送
- **第三方配送**：集成第三方配送平台
- **自提**：用户到店自提

### 3. 营销活动
- **优惠券**：支持使用优惠券
- **满减活动**：满一定金额减免
- **会员折扣**：会员专享折扣

### 4. 数据分析
- **订单统计**：分析订单量、客单价等
- **配送分析**：分析配送时间、延迟率等
- **用户行为**：分析用户下单习惯、偏好等

## 八、安全设计

### 1. 数据安全
- **敏感信息加密**：用户地址、手机号等敏感信息加密存储
- **订单数据保护**：防止订单信息泄露
- **支付信息安全**：确保支付信息不被窃取

### 2. 接口安全
- **身份验证**：所有API接口需要身份验证
- **权限控制**：不同角色（用户、商家、管理员）有不同权限
- **请求频率限制**：防止恶意请求和DoS攻击

### 3. 交易安全
- **订单防重**：防止重复下单
- **支付验证**：确保支付结果的真实性
- **配送验证**：验证配送员身份和配送过程

## 九、技术栈选择

### 后端
- **Spring Boot 2.7.x**：基础框架
- **MyBatis-Plus**：ORM框架
- **Spring Security**：权限管理
- **WebSocket**：实时通信
- **Redis**：缓存和消息队列
- **MySQL**：数据库

### 前端
- **Vue 3**：前端框架
- **Element Plus**：UI组件库
- **Axios**：HTTP请求
- **WebSocket**：实时通信
- **高德地图API**：地址和地图功能

### 第三方服务
- **微信支付**：在线支付
- **支付宝**：在线支付
- **高德地图**：地址解析和地图展示
- **短信服务**：订单通知

## 十、实施计划

### 1. 阶段一：核心功能实现
- 地址管理模块
- 订单管理模块（基本功能）
- 支付集成
- 基础配送管理

### 2. 阶段二：功能完善
- 配送状态追踪
- 实时通知
- 异常处理
- 数据统计

### 3. 阶段三：扩展功能
- 多商家支持
- 营销活动
- 第三方配送集成
- 移动端适配

## 总结

外卖功能设计需要覆盖订单全生命周期，确保流程顺畅、用户体验良好。核心是地址管理、订单状态流转和配送管理的实现，同时要考虑系统的可扩展性和安全性。通过合理的模块划分和技术选型，可以构建一个稳定、高效的外卖系统。