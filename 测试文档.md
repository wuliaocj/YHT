# 购物车功能完整流程文档

## 1. 功能概述

购物车功能是奶茶点餐系统中的核心功能之一，允许用户将商品添加到购物车中，以便后续结算。本次修改后，购物车功能支持多个规格选择，包括杯型、口味、温度和小料，均以数组形式传递。

## 2. 完整流程梳理

### 2.1 前端调用流程
1. **用户选择商品**：用户在前端选择商品，并设置购买数量
2. **选择规格**：用户选择杯型、口味、温度和小料等规格
3. **构建请求参数**：前端构建符合 AddCartVO 格式的请求参数
4. **发送请求**：前端通过 POST 请求调用 `/api/cart/add` 接口
5. **处理响应**：前端接收并处理后端返回的响应，显示添加结果

### 2.2 后端处理流程
1. **接收请求**：CartController 接收前端发送的 POST 请求
2. **参数校验**：Spring 框架自动校验请求参数的格式和必填项
3. **业务处理**：CartServiceImpl 的 addToCart 方法处理具体业务逻辑
4. **规格校验**：验证所有传入的规格 ID 是否有效
5. **价格计算**：根据商品基础价格和规格加价计算最终单价
6. **购物车记录处理**：检查是否存在相同商品和规格的购物车记录，存在则更新数量，不存在则新增
7. **返回响应**：返回处理结果给前端

### 2.3 数据库操作流程
1. **查询商品信息**：从 product 表查询商品基础信息和基础价格
2. **查询规格信息**：从 product_spec_price 表查询规格信息和加价
3. **查询购物车记录**：从 cart 表查询是否存在相同商品和规格的记录
4. **更新或插入记录**：根据查询结果更新或插入购物车记录

## 3. API 说明

### 3.1 加入购物车 API

#### API 端点
- **URL**: `http://localhost:8080/api/cart/add`
- **方法**: POST
- **Content-Type**: application/json

#### 请求参数 (AddCartVO)
| 参数名 | 类型 | 必填 | 描述 | 示例 |
|-------|------|------|------|------|
| userId | Integer | 是 | 用户ID | 1001 |
| productId | Integer | 是 | 商品ID | 10 |
| quantity | Integer | 是 | 购买数量，最小为1 | 2 |
| cupSpecIds | List<Long> | 是 | 杯型规格ID列表 | [20, 21] |
| tasteSpecIds | List<Long> | 是 | 口味规格ID列表 | [23, 24] |
| temperatureSpecIds | List<Long> | 是 | 温度规格ID列表 | [25, 26] |
| toppingSpecIds | List<Long> | 否 | 小料规格ID列表，无则传空列表 | [40, 41] |
| isSelected | Integer | 否 | 是否选中，默认1=选中 | 1 |

#### 请求示例
```json
{
  "userId": 1001,
  "productId": 10,
  "quantity": 2,
  "cupSpecIds": [20, 21],
  "tasteSpecIds": [23, 24],
  "temperatureSpecIds": [25, 26],
  "toppingSpecIds": [40, 41],
  "isSelected": 1
}
```

#### 响应格式
##### 成功响应
```json
{
  "code": 200,
  "msg": "加入购物车成功",
  "data": {
    "userId": 1001,
    "productId": 10,
    "quantity": 2,
    "cupSpecIds": [20, 21],
    "tasteSpecIds": [23, 24],
    "temperatureSpecIds": [25, 26],
    "toppingSpecIds": [40, 41],
    "isSelected": 1
  }
}
```

##### 错误响应
```json
{
  "code": 500,
  "msg": "系统异常：购买数量不能小于1",
  "data": null
}
```

### 3.2 其他购物车相关 API

#### 3.2.1 获取购物车列表
- **URL**: `http://localhost:8080/api/cart/{userId}`
- **方法**: GET
- **参数**: userId (路径参数)
- **响应**: 购物车列表

#### 3.2.2 更新购物车项
- **URL**: `http://localhost:8080/api/cart/update`
- **方法**: POST
- **参数**: 
  - cartId (必填): 购物车项ID
  - quantity (必填): 购买数量
  - selectedSpecs (可选): 选中的商品规格
  - selected (可选): 是否选中

#### 3.2.3 删除购物车项
- **URL**: `http://localhost:8080/api/cart/remove/{cartId}`
- **方法**: POST
- **参数**: cartId (路径参数)

#### 3.2.4 清空购物车
- **URL**: `http://localhost:8080/api/cart/clear/{userId}`
- **方法**: POST
- **参数**: userId (路径参数)

## 4. 代码分析

### 4.1 CartController.java

```java
@RestController
@RequestMapping("/api/cart")
public class CartController {

    private final CartService cartService;

    public CartController(CartService cartService) {
        this.cartService = cartService;
    }

    @PostMapping("/add")
    public HttpResult addToCart(@RequestBody AddCartVO addCartVO) throws JsonProcessingException {
        return HttpResult.ok("加入购物车成功", cartService.addToCart(addCartVO));
    }
    
    // 其他方法...
}
```

**分析**：
- 使用 `@RestController` 注解标记为 REST 控制器
- 使用 `@RequestMapping("/api/cart")` 设置基础路径
- `addToCart` 方法使用 `@PostMapping("/add")` 映射 POST 请求
- 使用 `@RequestBody` 接收请求体并自动转换为 AddCartVO 对象
- 调用 `cartService.addToCart` 处理业务逻辑
- 返回 `HttpResult.ok` 包装的成功响应

### 4.2 CartServiceImpl.java

```java
@Override
@Transactional(rollbackFor = Exception.class)
public AddCartVO addToCart(AddCartVO addCartVO) {
    // 1. 基础参数获取与校验
    Integer userId = addCartVO.getUserId();
    Integer productId = addCartVO.getProductId();
    Integer quantity = addCartVO.getQuantity();
    List<Long> cupSpecIds = addCartVO.getCupSpecIds();
    List<Long> tasteSpecIds = addCartVO.getTasteSpecIds();
    List<Long> temperatureSpecIds = addCartVO.getTemperatureSpecIds();
    List<Long> toppingSpecIds = addCartVO.getToppingSpecIds();

    if (quantity < 1) {
        throw new BusinessException("购买数量不能小于1");
    }

    // 2. 校验商品有效性
    Product product = productService.getById(productId);
    if (product == null || product.getStatus() == 0) {
        throw new BusinessException("商品不存在或已下架");
    }

    // 3. 查询并校验杯型规格
    String cupSpecNames = "";
    List<ProductSpecPrice> cupSpecList = List.of();
    if (!CollectionUtils.isEmpty(cupSpecIds)) {
        cupSpecList = productSpecPriceService.listByIds(cupSpecIds);
        // 过滤有效杯型规格
        cupSpecList = cupSpecList.stream()
                .filter(s -> "cup_type".equals(s.getSpecType()) && s.getStatus() == 1)
                .collect(Collectors.toList());
        // 校验是否有无效杯型
        if (cupSpecList.size() != cupSpecIds.size()) {
            throw new BusinessException("部分杯型规格无效或已下架");
        }
        // 拼接杯型名称
        cupSpecNames = cupSpecList.stream()
                .map(ProductSpecPrice::getSpecName)
                .collect(Collectors.joining("+"));
    }

    // 4. 查询并校验口味规格
    // 5. 查询并校验温度规格
    // 6. 查询并校验小料规格
    // （以上三段代码结构类似，省略）

    // 7. 构建规格相关字段
    Map<String, Object> specMap = new LinkedHashMap<>();
    specMap.put("cupSpecIds", cupSpecIds == null ? new ArrayList<>() : cupSpecIds);
    specMap.put("tasteSpecIds", tasteSpecIds == null ? new ArrayList<>() : tasteSpecIds);
    specMap.put("temperatureSpecIds", temperatureSpecIds == null ? new ArrayList<>() : temperatureSpecIds);
    specMap.put("toppingSpecIds", toppingSpecIds == null ? new ArrayList<>() : toppingSpecIds);

    // 8. 序列化规格信息
    String specIds = JsonUtil.toJson(specMap);

    // 9. 构建selectedSpecs
    StringBuilder selectedSpecsBuilder = new StringBuilder();
    if (!cupSpecNames.isEmpty()) {
        selectedSpecsBuilder.append(cupSpecNames);
    }
    if (!tasteSpecNames.isEmpty()) {
        if (selectedSpecsBuilder.length() > 0) {
            selectedSpecsBuilder.append("/");
        }
        selectedSpecsBuilder.append(tasteSpecNames);
    }
    if (!temperatureSpecNames.isEmpty()) {
        if (selectedSpecsBuilder.length() > 0) {
            selectedSpecsBuilder.append("/");
        }
        selectedSpecsBuilder.append(temperatureSpecNames);
    }
    if (!toppingSpecNames.isEmpty()) {
        if (selectedSpecsBuilder.length() > 0) {
            selectedSpecsBuilder.append("/");
        }
        selectedSpecsBuilder.append(toppingSpecNames);
    }
    String selectedSpecs = selectedSpecsBuilder.toString();

    // 10. 计算商品单价
    BigDecimal cupAdd = cupSpecList.stream()
            .map(ProductSpecPrice::getPriceAdd)
            .reduce(BigDecimal.ZERO, BigDecimal::add);
    BigDecimal tasteAdd = tasteSpecList.stream()
            .map(ProductSpecPrice::getPriceAdd)
            .reduce(BigDecimal.ZERO, BigDecimal::add);
    BigDecimal temperatureAdd = temperatureSpecList.stream()
            .map(ProductSpecPrice::getPriceAdd)
            .reduce(BigDecimal.ZERO, BigDecimal::add);
    BigDecimal toppingAdd = toppingSpecList.stream()
            .map(ProductSpecPrice::getPriceAdd)
            .reduce(BigDecimal.ZERO, BigDecimal::add);
    BigDecimal unitPrice = product.getBasePrice().add(cupAdd).add(tasteAdd).add(temperatureAdd).add(toppingAdd);

    // 11. 检查是否存在同商品+同规格的购物车记录
    LambdaQueryWrapper<Cart> wrapper = new LambdaQueryWrapper<>();
    wrapper.eq(Cart::getUserId, userId)
            .eq(Cart::getProductId, productId)
            .eq(Cart::getSpecIds, specIds);
    Cart existCart = this.getOne(wrapper);

    if (existCart != null) {
        // 已存在：更新数量
        existCart.setQuantity(existCart.getQuantity() + quantity);
        existCart.setUpdateTime(LocalDateTime.now());
        this.updateById(existCart);
    } else {
        // 不存在：新增购物车记录
        try {
            Cart cart = new Cart();
            cart.setUserId(userId);
            cart.setProductId(productId);
            cart.setProductName(product.getName());
            cart.setProductImage(product.getMainImage());
            cart.setSpecIds(specIds);
            cart.setQuantity(quantity);
            cart.setSelectedSpecs(selectedSpecs);
            cart.setUnitPrice(unitPrice);
            cart.setIsSelected(addCartVO.getIsSelected() == null ? 1 : addCartVO.getIsSelected());
            cart.setCreateTime(LocalDateTime.now());
            cart.setUpdateTime(LocalDateTime.now());

            cartMapper.insertCartWithSpecIds(cart);
        } catch (DuplicateKeyException e) {
            // 捕获并发导致的重复插入异常，再次查询并更新
            Cart concurrentCart = this.getOne(wrapper);
            if (concurrentCart != null) {
                concurrentCart.setQuantity(concurrentCart.getQuantity() + quantity);
                concurrentCart.setUpdateTime(LocalDateTime.now());
                this.updateById(concurrentCart);
            }
        }
    }
    return addCartVO;
}
```

**分析**：
- 使用 `@Transactional` 注解确保事务一致性
- 实现了完整的业务逻辑，包括参数校验、商品有效性检查、规格校验、价格计算和购物车记录处理
- 支持多个规格选择，通过 `List<Long>` 类型接收规格 ID 列表
- 对每个规格类型进行单独的校验和处理
- 使用 `JsonUtil.toJson` 序列化规格信息为 JSON 字符串，存储在 `specIds` 字段中
- 构建 `selectedSpecs` 字段，用于前端显示规格信息
- 计算商品单价时，累加所有规格的加价
- 处理购物车记录时，考虑了并发情况，使用 `try-catch` 捕获 `DuplicateKeyException`

### 4.3 CartMapper.java

```java
@Mapper
public interface CartMapper extends BaseMapper<Cart> {
    Cart selectById(@Param("id") Integer id);
    List<Cart> selectByUserId(@Param("userId") Integer userId);
    Cart selectByUserAndProduct(@Param("userId") Integer userId, @Param("productId") Integer productId);
    int insert(Cart cart);
    int update(Cart cart);
    int deleteById(@Param("id") Integer id);
    int deleteByUserId(@Param("userId") Integer userId);
    void insertCartWithSpecIds(Cart cart);
}
```

**分析**：
- 继承自 MyBatis-Plus 的 `BaseMapper`，提供基础的 CRUD 方法
- 定义了多个自定义方法，用于特定场景的查询和操作
- `insertCartWithSpecIds` 方法专门用于插入包含 `specIds` 字段的购物车记录

### 4.4 CartMapper.xml

```xml
<insert id="insertCartWithSpecIds">
    INSERT INTO cart (
        user_id, product_id, product_name, product_image,
        spec_ids, quantity, selected_specs, unit_price,
        is_selected, create_time, update_time
    ) VALUES (
                 #{userId}, #{productId}, #{productName}, #{productImage},
                 #{specIds}, #{quantity}, #{selectedSpecs}, #{unitPrice},
                 #{isSelected}, #{createTime}, #{updateTime}
             )
</insert>
```

**分析**：
- 定义了 `insertCartWithSpecIds` 方法的具体 SQL 实现
- 插入购物车记录时，包含了所有必要的字段，包括 `specIds` 和 `unitPrice`

## 5. 数据库表结构

### 5.1 购物车表 (cart)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 购物车ID |
| user_id | INT | NOT NULL | 用户ID |
| product_id | INT | NOT NULL | 商品ID |
| product_name | VARCHAR(255) | NOT NULL | 商品名称（冗余） |
| product_image | VARCHAR(255) | NOT NULL | 商品图片（冗余） |
| spec_ids | TEXT | NOT NULL | 规格ID组合（JSON格式） |
| quantity | INT | NOT NULL | 购买数量 |
| selected_specs | VARCHAR(255) | NOT NULL | 选中的商品规格（显示用） |
| unit_price | DECIMAL(10,2) | NOT NULL | 商品单价 |
| is_selected | TINYINT | NOT NULL, DEFAULT 1 | 是否选中（1=选中，0=未选中） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |
| total_price | DECIMAL(10,2) | NOT NULL | 商品总价 |

### 5.2 商品规格价格表 (product_spec_price)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | BIGINT | PRIMARY KEY, AUTO_INCREMENT | 规格ID |
| product_id | INT | NOT NULL | 商品ID |
| spec_type | VARCHAR(50) | NOT NULL | 规格类型（cup_type/taste/temperature/topping） |
| spec_name | VARCHAR(100) | NOT NULL | 规格名称 |
| price_add | DECIMAL(10,2) | NOT NULL, DEFAULT 0 | 规格加价 |
| status | TINYINT | NOT NULL, DEFAULT 1 | 状态（1=启用，0=禁用） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

### 5.3 商品表 (product)

| 字段名 | 数据类型 | 约束 | 描述 |
|-------|---------|------|------|
| id | INT | PRIMARY KEY, AUTO_INCREMENT | 商品ID |
| name | VARCHAR(255) | NOT NULL | 商品名称 |
| base_price | DECIMAL(10,2) | NOT NULL | 商品基础价格 |
| main_image | VARCHAR(255) | NOT NULL | 商品主图 |
| status | TINYINT | NOT NULL, DEFAULT 1 | 状态（1=启用，0=禁用） |
| create_time | DATETIME | NOT NULL | 创建时间 |
| update_time | DATETIME | NOT NULL | 更新时间 |

## 6. 测试示例

### 6.1 CURL 测试示例

```bash
# 测试添加购物车
curl -X POST http://localhost:8080/api/cart/add \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 1001,
    "productId": 10,
    "quantity": 2,
    "cupSpecIds": [20, 21],
    "tasteSpecIds": [23, 24],
    "temperatureSpecIds": [25, 26],
    "toppingSpecIds": [40, 41],
    "isSelected": 1
  }'

# 测试获取购物车列表
curl -X GET http://localhost:8080/api/cart/1001

# 测试更新购物车项
curl -X POST "http://localhost:8080/api/cart/update?cartId=1&quantity=3&selected=true"

# 测试删除购物车项
curl -X POST http://localhost:8080/api/cart/remove/1

# 测试清空购物车
curl -X POST http://localhost:8080/api/cart/clear/1001
```

### 6.2 Postman 测试示例

1. **添加购物车**
   - 方法: POST
   - URL: `http://localhost:8080/api/cart/add`
   - Body: raw JSON
   - 请求体:
     ```json
     {
       "userId": 1001,
       "productId": 10,
       "quantity": 2,
       "cupSpecIds": [20, 21],
       "tasteSpecIds": [23, 24],
       "temperatureSpecIds": [25, 26],
       "toppingSpecIds": [40, 41],
       "isSelected": 1
     }
     ```

2. **获取购物车列表**
   - 方法: GET
   - URL: `http://localhost:8080/api/cart/1001`

3. **更新购物车项**
   - 方法: POST
   - URL: `http://localhost:8080/api/cart/update`
   - Params:
     - cartId: 1
     - quantity: 3
     - selected: true

4. **删除购物车项**
   - 方法: POST
   - URL: `http://localhost:8080/api/cart/remove/1`

5. **清空购物车**
   - 方法: POST
   - URL: `http://localhost:8080/api/cart/clear/1001`

## 7. 连锁反应和注意事项

### 7.1 前端修改
- **请求参数格式**：前端需要将杯型、口味、温度参数改为数组格式，不再使用单个 ID 值
- **规格选择组件**：前端需要更新规格选择组件，支持多个规格的选择
- **购物车列表展示**：前端需要能够解析新的 specIds 格式，以正确显示商品规格
- **价格计算**：前端在显示价格时需要考虑所有规格的加价

### 7.2 后端修改
- **API 文档更新**：需要更新 API 文档，反映字段类型的变化
- **其他服务调用**：如果有其他服务调用了 addToCart 方法，需要修改参数传递方式
- **测试用例更新**：需要更新相关的测试用例，以适应字段类型的变化

### 7.3 数据库影响
- **数据存储**：specIds 字段的存储格式发生了变化，但存储方式不变（仍然是 JSON 字符串）
- **查询逻辑**：基于 specIds 的查询逻辑仍然有效，因为只是改变了其内部结构

## 8. 常见问题和解决方案

### 8.1 常见错误
1. **购买数量不能小于1**：当 quantity < 1 时返回
2. **商品不存在或已下架**：当商品不存在或状态为 0 时返回
3. **部分杯型规格无效或已下架**：当杯型规格 ID 列表中存在无效或已下架的规格时返回
4. **部分口味规格无效或已下架**：当口味规格 ID 列表中存在无效或已下架的规格时返回
5. **部分温度规格无效或已下架**：当温度规格 ID 列表中存在无效或已下架的规格时返回
6. **部分小料规格无效或已下架**：当小料规格 ID 列表中存在无效或已下架的规格时返回

### 8.2 解决方案
1. **前端验证**：在发送请求前，前端应验证购买数量是否大于等于 1
2. **规格选择限制**：前端应只显示有效的规格选项，避免用户选择无效的规格
3. **错误处理**：前端应妥善处理后端返回的错误信息，显示友好的提示
4. **并发处理**：后端已经处理了并发情况下的重复插入问题，前端无需额外处理

## 9. 总结

本次购物车功能的修改，将杯型、口味、温度字段从单个 Long 类型改为 List<Long> 类型，以支持多个规格选择。修改后，购物车功能更加灵活，能够满足用户对多种规格组合的需求。

完整的流程包括前端调用、后端处理和数据库操作三个主要环节，每个环节都有详细的实现和处理逻辑。通过本文档的说明，开发者可以清晰地了解购物车功能的实现细节和使用方法，以便更好地集成和测试该功能。

同时，本文档也分析了修改可能带来的连锁反应和注意事项，以及常见问题和解决方案，为开发者提供了全面的参考。