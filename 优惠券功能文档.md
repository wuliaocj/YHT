# 优惠券功能技术文档

## 1. 功能概述

优惠券功能是奶茶点餐系统的重要营销工具，允许管理员创建和管理优惠券，用户可以领取和使用优惠券享受折扣。

### 1.1 核心功能
- **优惠券管理**：管理员可以创建、查看、删除优惠券
- **优惠券领取**：用户可以领取可用的优惠券
- **优惠券使用**：用户在下单时可以使用优惠券，系统自动计算优惠金额
- **优惠券查询**：用户可以查看已领取的优惠券和可用优惠券

### 1.2 优惠券类型
- **满减券**：达到指定金额后减免固定金额
- **折扣券**：按比例折扣，可设置最大优惠金额

## 2. 技术实现

### 2.1 数据库设计

#### 2.1.1 优惠券表 (`coupon`)
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | int | 优惠券ID |
| name | varchar(100) | 优惠券名称 |
| type | tinyint | 类型：0-满减券，1-折扣券 |
| value | decimal(10,2) | 优惠券面额/折扣值 |
| min_amount | decimal(10,2) | 使用门槛 |
| max_discount | decimal(10,2) | 最大优惠金额 |
| start_time | datetime | 开始时间 |
| end_time | datetime | 结束时间 |
| status | tinyint | 状态：0-禁用，1-启用 |
| total_count | int | 发放总量 |
| remaining_count | int | 剩余数量 |
| usage_count | int | 已使用数量 |
| apply_scope | tinyint | 适用范围：0-全场通用，1-指定商品，2-指定分类 |
| apply_content | text | 适用内容（JSON格式） |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

#### 2.1.2 用户优惠券表 (`user_coupon`)
| 字段名 | 数据类型 | 描述 |
|-------|---------|------|
| id | int | 用户优惠券ID |
| user_id | int | 用户ID |
| coupon_id | int | 优惠券ID |
| coupon_code | varchar(50) | 优惠券码 |
| status | tinyint | 状态：0-未使用，1-已使用，2-已过期 |
| used_time | datetime | 使用时间 |
| used_order_id | int | 使用的订单ID |
| receive_time | datetime | 领取时间 |
| expire_time | datetime | 过期时间 |

### 2.2 核心代码文件

#### 2.2.1 CouponController.java
处理优惠券相关的HTTP请求，包括管理员接口和用户接口。

**管理员接口**：
- `GET /api/admin/coupon/list`：获取所有优惠券列表
- `POST /api/admin/coupon/save`：创建/更新优惠券
- `POST /api/admin/coupon/delete/{id}`：删除优惠券

**用户接口**：
- `GET /api/coupon/available/{userId}`：获取可用优惠券列表
- `GET /api/coupon/user/{userId}`：获取用户所有优惠券
- `GET /api/coupon/user/available/{userId}`：获取用户可用优惠券
- `POST /api/coupon/receive`：领取优惠券

#### 2.2.2 CouponServiceImpl.java
实现优惠券的核心业务逻辑，包括：
- `listAll()`：获取所有优惠券
- `save(Coupon coupon)`：保存优惠券
- `delete(Integer id)`：删除优惠券
- `listAvailableCoupons(Integer userId)`：获取可用优惠券
- `listUserCoupons(Integer userId)`：获取用户所有优惠券
- `listUserAvailableCoupons(Integer userId)`：获取用户可用优惠券
- `receiveCoupon(Integer userId, Integer couponId)`：领取优惠券
- `useCoupon(Integer userId, Integer couponId, Integer orderId)`：使用优惠券

#### 2.2.3 OrderServiceImpl.java
集成优惠券使用逻辑，在订单支付成功后使用优惠券：
```java
// 当订单状态变为支付成功时，生成取餐码并添加积分
if (status == 2 && oldStatus != 2) {
    String takeCode = generateTakeCode();
    order.setTakeCode(takeCode);
    
    // 添加积分
    try {
        userPointService.addPointByOrder(order.getUserId(), orderId, order.getActualAmount());
    } catch (Exception e) {
        log.error("添加积分失败：", e);
        // 积分添加失败不影响订单状态更新
    }
    
    // 使用优惠券（如果有）
    Integer couponId = order.getCouponId();
    if (couponId != null) {
        try {
            couponService.useCoupon(order.getUserId(), couponId, orderId);
        } catch (Exception e) {
            log.error("使用优惠券失败：", e);
            // 优惠券使用失败不影响订单状态更新
        }
    }
}
```

## 3. API接口文档

### 3.1 管理员接口

#### 3.1.1 获取优惠券列表
- **URL**：`/api/admin/coupon/list`
- **方法**：`GET`
- **权限**：需要ADMIN角色
- **返回值**：
```json
{
  "code": 200,
  "msg": null,
  "data": [
    {
      "id": 1,
      "name": "新人满减券",
      "type": 0,
      "value": 5.00,
      "minAmount": 20.00,
      "totalCount": 1000,
      "remainingCount": 1000,
      "limitPerUser": 1,
      "validityType": 1,
      "startTime": "2026-01-01",
      "endTime": "2026-12-31",
      "validDays": null,
      "applicableProducts": null,
      "applicableCategories": null,
      "status": 1,
      "createTime": "2026-01-29 09:39:03"
    }
  ]
}
```

#### 3.1.2 创建/更新优惠券
- **URL**：`/api/admin/coupon/save`
- **方法**：`POST`
- **权限**：需要ADMIN角色
- **请求体**：
```json
{
  "name": "测试满减券",
  "type": 0,
  "value": 5.00,
  "min_amount": 20.00,
  "total_count": 100,
  "remaining_count": 100,
  "status": 1,
  "apply_scope": 0
}
```
- **返回值**：
```json
{
  "code": 200,
  "msg": null,
  "data": {
    "id": 4,
    "name": "测试满减券",
    "type": 0,
    "value": 5.00,
    "minAmount": 20.00,
    "totalCount": 100,
    "remainingCount": 100,
    "limitPerUser": 1,
    "validityType": 1,
    "startTime": null,
    "endTime": null,
    "validDays": null,
    "applicableProducts": null,
    "applicableCategories": null,
    "status": 1,
    "createTime": "2026-02-27 10:00:00"
  }
}
```

#### 3.1.3 删除优惠券
- **URL**：`/api/admin/coupon/delete/{id}`
- **方法**：`POST`
- **权限**：需要ADMIN角色
- **返回值**：
```json
{
  "code": 200,
  "msg": "删除成功",
  "data": null
}
```

### 3.2 用户接口

#### 3.2.1 领取优惠券
- **URL**：`/api/coupon/receive`
- **方法**：`POST`
- **参数**：
  - `userId`：用户ID
  - `couponId`：优惠券ID
- **返回值**：
```json
{
  "code": 200,
  "msg": "领取成功",
  "data": null
}
```

#### 3.2.2 获取用户可用优惠券
- **URL**：`/api/coupon/user/available/{userId}`
- **方法**：`GET`
- **返回值**：
```json
{
  "code": 200,
  "msg": null,
  "data": [
    {
      "id": 1,
      "userId": 2,
      "couponId": 1,
      "couponCode": "BKDM01PMVCGN",
      "status": 1,
      "usedTime": null,
      "usedOrderId": null,
      "receiveTime": "2026-02-27T10:00:03",
      "expireTime": "2026-12-31T23:59:59"
    }
  ]
}
```

#### 3.2.3 获取用户所有优惠券
- **URL**：`/api/coupon/user/{userId}`
- **方法**：`GET`
- **返回值**：
```json
{
  "code": 200,
  "msg": null,
  "data": [
    {
      "id": 1,
      "userId": 2,
      "couponId": 1,
      "couponCode": "BKDM01PMVCGN",
      "status": 1,
      "usedTime": null,
      "usedOrderId": null,
      "receiveTime": "2026-02-27T10:00:03",
      "expireTime": "2026-12-31T23:59:59"
    }
  ]
}
```

## 4. 使用流程

### 4.1 管理员操作流程
1. **登录系统**：使用管理员账号登录系统
2. **创建优惠券**：通过管理后台创建优惠券，设置优惠券类型、面额、使用门槛等信息
3. **管理优惠券**：查看、编辑或删除已创建的优惠券

### 4.2 用户操作流程
1. **登录系统**：使用微信小程序或测试账号登录系统
2. **浏览优惠券**：查看可领取的优惠券列表
3. **领取优惠券**：选择合适的优惠券进行领取
4. **下单使用**：在下单时选择已领取的优惠券，系统自动计算优惠金额
5. **查看优惠券**：查看已领取的优惠券和使用状态

## 5. 测试结果

### 5.1 测试环境
- **系统**：Spring Boot 2.7.18
- **数据库**：MySQL 8.0
- **JDK**：Java 11

### 5.2 测试步骤
1. **登录获取令牌**：使用测试账号登录获取JWT令牌
2. **获取优惠券列表**：管理员获取所有优惠券
3. **领取优惠券**：用户领取优惠券
4. **查询优惠券**：用户查询可用优惠券和所有优惠券

### 5.3 测试结果
- **登录成功**：成功获取JWT令牌
- **获取优惠券列表**：返回3个预设优惠券
- **领取优惠券**：成功领取ID为1的优惠券
- **查询可用优惠券**：返回用户可用的优惠券
- **查询所有优惠券**：返回用户所有的优惠券

### 5.4 测试脚本
```powershell
# 测试优惠券完整流程

# 1. 测试登录获取JWT令牌
Write-Host "=== 1. 测试登录获取JWT令牌 ==="
try {
    $loginResponse = Invoke-RestMethod -Uri "http://localhost:8080/api/user/test/login" -Method POST -Headers @{"Content-Type"="application/json"} -Body '{"username":"test","password":"123456"}'
    $token = $loginResponse.data.token
    Write-Host "登录成功，获取到令牌: $token"
} catch {
    Write-Host "登录失败: $($_.Exception.Message)"
    exit 1
}

# 2. 管理员创建优惠券
Write-Host "\n=== 2. 测试管理员创建优惠券 ==="
try {
    $createCouponResponse = Invoke-RestMethod -Uri "http://localhost:8080/api/admin/coupon/save" -Method POST -Headers @{"Content-Type"="application/json"; "Authorization"="Bearer $token"} -Body '{"name":"测试满减券","type":0,"value":5.00,"min_amount":20.00,"total_count":100,"remaining_count":100,"status":1,"apply_scope":0}'
    Write-Host "创建优惠券成功: $($createCouponResponse | ConvertTo-Json -Depth 3)"
} catch {
    Write-Host "创建优惠券失败: $($_.Exception.Message)"
}

# 3. 管理员获取优惠券列表
Write-Host "\n=== 3. 测试管理员获取优惠券列表 ==="
try {
    $couponListResponse = Invoke-RestMethod -Uri "http://localhost:8080/api/admin/coupon/list" -Method GET -Headers @{"Authorization"="Bearer $token"}
    Write-Host "获取优惠券列表成功: $($couponListResponse | ConvertTo-Json -Depth 3)"
} catch {
    Write-Host "获取优惠券列表失败: $($_.Exception.Message)"
}

# 4. 用户领取优惠券（使用测试用户ID 2）
Write-Host "\n=== 4. 测试用户领取优惠券 ==="
try {
    $receiveCouponResponse = Invoke-RestMethod -Uri "http://localhost:8080/api/coupon/receive?userId=2&couponId=1" -Method POST -Headers @{"Authorization"="Bearer $token"}
    Write-Host "领取优惠券成功: $($receiveCouponResponse | ConvertTo-Json -Depth 3)"
} catch {
    Write-Host "领取优惠券失败: $($_.Exception.Message)"
}

# 5. 用户获取可用优惠券
Write-Host "\n=== 5. 测试用户获取可用优惠券 ==="
try {
    $availableCouponsResponse = Invoke-RestMethod -Uri "http://localhost:8080/api/coupon/user/available/2" -Method GET -Headers @{"Authorization"="Bearer $token"}
    Write-Host "获取可用优惠券成功: $($availableCouponsResponse | ConvertTo-Json -Depth 3)"
} catch {
    Write-Host "获取可用优惠券失败: $($_.Exception.Message)"
}

# 6. 用户获取所有优惠券
Write-Host "\n=== 6. 测试用户获取所有优惠券 ==="
try {
    $allCouponsResponse = Invoke-RestMethod -Uri "http://localhost:8080/api/coupon/user/2" -Method GET -Headers @{"Authorization"="Bearer $token"}
    Write-Host "获取所有优惠券成功: $($allCouponsResponse | ConvertTo-Json -Depth 3)"
} catch {
    Write-Host "获取所有优惠券失败: $($_.Exception.Message)"
}
```

## 6. 总结

优惠券功能已成功实现，包括以下特点：
- **完整的优惠券管理系统**：支持管理员创建、查看、删除优惠券
- **灵活的优惠券规则**：支持满减券和折扣券，可设置使用门槛、有效期等
- **用户友好的操作流程**：用户可以方便地领取和使用优惠券
- **系统集成**：与订单系统无缝集成，在订单支付成功后自动使用优惠券

优惠券功能的实现将有助于提升用户体验，增加系统的营销能力，促进用户消费。