<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OrderItemMapper">

    <resultMap id="OrderItemResultMap" type="com.example.demo.domain.OrderItem">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="order_no" property="orderNo"/>
        <result column="product_id" property="productId"/>
        <result column="product_name" property="productName"/>
        <result column="product_image" property="productImage"/>
        <result column="spec_info" property="specInfo"/>
        <result column="unit_price" property="unitPrice"/>
        <result column="quantity" property="quantity"/>
        <result column="total_price" property="totalPrice"/>
        <result column="rating" property="rating"/>
        <result column="review" property="review"/>
        <result column="review_time" property="reviewTime"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="selectByOrderId" resultMap="OrderItemResultMap">
        select * from order_item where order_id = #{orderId}
    </select>

    <insert id="insertBatch">
        insert into order_item(order_id, order_no, product_id, product_name, product_image,
                               spec_info, unit_price, quantity, total_price)
        values
        <foreach collection="items" item="item" separator=",">
            (#{item.orderId}, #{item.orderNo}, #{item.productId}, #{item.productName}, #{item.productImage},
             #{item.specInfo}, #{item.unitPrice}, #{item.quantity}, #{item.totalPrice})
        </foreach>
    </insert>
</mapper>


