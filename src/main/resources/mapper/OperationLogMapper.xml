<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.OperationLogMapper">

    <resultMap id="OperationLogResultMap" type="com.example.demo.domain.OperationLog">
        <id column="id" property="id"/>
        <result column="admin_id" property="adminId"/>
        <result column="module" property="module"/>
        <result column="operation" property="operation"/>
        <result column="method" property="method"/>
        <result column="params" property="params"/>
        <result column="ip" property="ip"/>
        <result column="user_agent" property="userAgent"/>
        <result column="status" property="status"/>
        <result column="error_message" property="errorMessage"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into operation_log(admin_id, module, operation, method, params, ip, user_agent, status, error_message, create_time)
        values (#{adminId}, #{module}, #{operation}, #{method}, #{params}, #{ip}, #{userAgent}, #{status}, #{errorMessage}, #{createTime})
    </insert>

    <select id="selectByCondition" resultMap="OperationLogResultMap">
        select * from operation_log
        where 1=1
        <if test="module != null and module != ''">
            and module = #{module}
        </if>
        <if test="adminId != null">
            and admin_id = #{adminId}
        </if>
        <if test="startTime != null and startTime != ''">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            and create_time &lt;= #{endTime}
        </if>
        order by create_time desc
    </select>

    <select id="selectLoginLogs" resultMap="OperationLogResultMap">
        select * from operation_log
        where module = '登录'
        <if test="username != null and username != ''">
            and params like concat('%', #{username}, '%')
        </if>
        <if test="ip != null and ip != ''">
            and ip = #{ip}
        </if>
        <if test="startTime != null and startTime != ''">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            and create_time &lt;= #{endTime}
        </if>
        order by create_time desc
    </select>

    <select id="selectByConditionWithPage" resultMap="OperationLogResultMap">
        select * from operation_log
        where 1=1
        <if test="module != null and module != ''">
            and module = #{module}
        </if>
        <if test="adminId != null">
            and admin_id = #{adminId}
        </if>
        <if test="startTime != null and startTime != ''">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            and create_time &lt;= #{endTime}
        </if>
        order by create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="selectLoginLogsWithPage" resultMap="OperationLogResultMap">
        select * from operation_log
        where module = '登录'
        <if test="username != null and username != ''">
            and params like concat('%', #{username}, '%')
        </if>
        <if test="ip != null and ip != ''">
            and ip = #{ip}
        </if>
        <if test="startTime != null and startTime != ''">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            and create_time &lt;= #{endTime}
        </if>
        order by create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="selectOperationLogCount" resultType="java.lang.Long">
        select count(*) from operation_log
        where 1=1
        <if test="module != null and module != ''">
            and module = #{module}
        </if>
        <if test="adminId != null">
            and admin_id = #{adminId}
        </if>
        <if test="startTime != null and startTime != ''">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            and create_time &lt;= #{endTime}
        </if>
    </select>

    <select id="selectLoginLogCount" resultType="java.lang.Long">
        select count(*) from operation_log
        where module = '登录'
        <if test="username != null and username != ''">
            and params like concat('%', #{username}, '%')
        </if>
        <if test="ip != null and ip != ''">
            and ip = #{ip}
        </if>
        <if test="startTime != null and startTime != ''">
            and create_time &gt;= #{startTime}
        </if>
        <if test="endTime != null and endTime != ''">
            and create_time &lt;= #{endTime}
        </if>
    </select>
</mapper>